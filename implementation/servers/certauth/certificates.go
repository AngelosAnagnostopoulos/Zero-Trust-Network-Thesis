package main

import "fmt"

func main() {
	// Create a server and keep it running
	// Connect to the device inventory database
	// Fetch data on all registered devices
	// Create a certificate for each one of the devices
	// Connect to the device certificate storage
	// Create or update the entries for the devices accordingly
	fmt.Println("Hi from certificates!")
}
